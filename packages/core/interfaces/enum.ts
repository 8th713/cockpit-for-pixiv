export enum AsyncStatus {
  Loading,
  Success,
  Failure
}

export enum FitStatus {
  NONE,
  COVER,
  CONTAIN
}

export enum SpreadStatus {
  NONE,
  SPREAD,
  SPREAD_SHIFT
}

export enum Column {
  TOTAL = 'total',
  NAME = 'name'
}

export enum Direction {
  ASC = '↓',
  DESC = '↑'
}
